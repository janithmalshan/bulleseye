import React from "react";

export const UiMainWrapper = (props) => {
    return (
        <main {...props} className="ui-main"></main>
    )
};

export const UiContentWrapper = (props) => {
    return (
        <div {...props} className="ui-content"></div>
    )
}
